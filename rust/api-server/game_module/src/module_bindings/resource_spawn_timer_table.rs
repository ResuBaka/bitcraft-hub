// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

// This was generated using spacetimedb cli version 1.2.0 (commit fb41e50eb73573b70eea532aeb6158eaac06fae0).

#![allow(unused, clippy::all)]
use super::resource_spawn_timer_type::ResourceSpawnTimer;
use super::small_hex_tile_message_type::SmallHexTileMessage;
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

/// Table handle for the table `resource_spawn_timer`.
///
/// Obtain a handle from the [`ResourceSpawnTimerTableAccess::resource_spawn_timer`] method on [`super::RemoteTables`],
/// like `ctx.db.resource_spawn_timer()`.
///
/// Users are encouraged not to explicitly reference this type,
/// but to directly chain method calls,
/// like `ctx.db.resource_spawn_timer().on_insert(...)`.
pub struct ResourceSpawnTimerTableHandle<'ctx> {
    imp: __sdk::TableHandle<ResourceSpawnTimer>,
    ctx: std::marker::PhantomData<&'ctx super::RemoteTables>,
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the table `resource_spawn_timer`.
///
/// Implemented for [`super::RemoteTables`].
pub trait ResourceSpawnTimerTableAccess {
    #[allow(non_snake_case)]
    /// Obtain a [`ResourceSpawnTimerTableHandle`], which mediates access to the table `resource_spawn_timer`.
    fn resource_spawn_timer(&self) -> ResourceSpawnTimerTableHandle<'_>;
}

impl ResourceSpawnTimerTableAccess for super::RemoteTables {
    fn resource_spawn_timer(&self) -> ResourceSpawnTimerTableHandle<'_> {
        ResourceSpawnTimerTableHandle {
            imp: self
                .imp
                .get_table::<ResourceSpawnTimer>("resource_spawn_timer"),
            ctx: std::marker::PhantomData,
        }
    }
}

pub struct ResourceSpawnTimerInsertCallbackId(__sdk::CallbackId);
pub struct ResourceSpawnTimerDeleteCallbackId(__sdk::CallbackId);

impl<'ctx> __sdk::Table for ResourceSpawnTimerTableHandle<'ctx> {
    type Row = ResourceSpawnTimer;
    type EventContext = super::EventContext;

    fn count(&self) -> u64 {
        self.imp.count()
    }
    fn iter(&self) -> impl Iterator<Item = ResourceSpawnTimer> + '_ {
        self.imp.iter()
    }

    type InsertCallbackId = ResourceSpawnTimerInsertCallbackId;

    fn on_insert(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row) + Send + 'static,
    ) -> ResourceSpawnTimerInsertCallbackId {
        ResourceSpawnTimerInsertCallbackId(self.imp.on_insert(Box::new(callback)))
    }

    fn remove_on_insert(&self, callback: ResourceSpawnTimerInsertCallbackId) {
        self.imp.remove_on_insert(callback.0)
    }

    type DeleteCallbackId = ResourceSpawnTimerDeleteCallbackId;

    fn on_delete(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row) + Send + 'static,
    ) -> ResourceSpawnTimerDeleteCallbackId {
        ResourceSpawnTimerDeleteCallbackId(self.imp.on_delete(Box::new(callback)))
    }

    fn remove_on_delete(&self, callback: ResourceSpawnTimerDeleteCallbackId) {
        self.imp.remove_on_delete(callback.0)
    }
}

#[doc(hidden)]
pub(super) fn register_table(client_cache: &mut __sdk::ClientCache<super::RemoteModule>) {
    let _table = client_cache.get_or_make_table::<ResourceSpawnTimer>("resource_spawn_timer");
    _table.add_unique_constraint::<u64>("scheduled_id", |row| &row.scheduled_id);
}
pub struct ResourceSpawnTimerUpdateCallbackId(__sdk::CallbackId);

impl<'ctx> __sdk::TableWithPrimaryKey for ResourceSpawnTimerTableHandle<'ctx> {
    type UpdateCallbackId = ResourceSpawnTimerUpdateCallbackId;

    fn on_update(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row, &Self::Row) + Send + 'static,
    ) -> ResourceSpawnTimerUpdateCallbackId {
        ResourceSpawnTimerUpdateCallbackId(self.imp.on_update(Box::new(callback)))
    }

    fn remove_on_update(&self, callback: ResourceSpawnTimerUpdateCallbackId) {
        self.imp.remove_on_update(callback.0)
    }
}

#[doc(hidden)]
pub(super) fn parse_table_update(
    raw_updates: __ws::TableUpdate<__ws::BsatnFormat>,
) -> __sdk::Result<__sdk::TableUpdate<ResourceSpawnTimer>> {
    __sdk::TableUpdate::parse_table_update(raw_updates).map_err(|e| {
        __sdk::InternalError::failed_parse("TableUpdate<ResourceSpawnTimer>", "TableUpdate")
            .with_cause(e)
            .into()
    })
}

/// Access to the `scheduled_id` unique index on the table `resource_spawn_timer`,
/// which allows point queries on the field of the same name
/// via the [`ResourceSpawnTimerScheduledIdUnique::find`] method.
///
/// Users are encouraged not to explicitly reference this type,
/// but to directly chain method calls,
/// like `ctx.db.resource_spawn_timer().scheduled_id().find(...)`.
pub struct ResourceSpawnTimerScheduledIdUnique<'ctx> {
    imp: __sdk::UniqueConstraintHandle<ResourceSpawnTimer, u64>,
    phantom: std::marker::PhantomData<&'ctx super::RemoteTables>,
}

impl<'ctx> ResourceSpawnTimerTableHandle<'ctx> {
    /// Get a handle on the `scheduled_id` unique index on the table `resource_spawn_timer`.
    pub fn scheduled_id(&self) -> ResourceSpawnTimerScheduledIdUnique<'ctx> {
        ResourceSpawnTimerScheduledIdUnique {
            imp: self.imp.get_unique_constraint::<u64>("scheduled_id"),
            phantom: std::marker::PhantomData,
        }
    }
}

impl<'ctx> ResourceSpawnTimerScheduledIdUnique<'ctx> {
    /// Find the subscribed row whose `scheduled_id` column value is equal to `col_val`,
    /// if such a row is present in the client cache.
    pub fn find(&self, col_val: &u64) -> Option<ResourceSpawnTimer> {
        self.imp.find(col_val)
    }
}
