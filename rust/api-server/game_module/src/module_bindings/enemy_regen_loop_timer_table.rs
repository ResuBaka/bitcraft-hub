// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use super::enemy_regen_loop_timer_type::EnemyRegenLoopTimer;
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

/// Table handle for the table `enemy_regen_loop_timer`.
///
/// Obtain a handle from the [`EnemyRegenLoopTimerTableAccess::enemy_regen_loop_timer`] method on [`super::RemoteTables`],
/// like `ctx.db.enemy_regen_loop_timer()`.
///
/// Users are encouraged not to explicitly reference this type,
/// but to directly chain method calls,
/// like `ctx.db.enemy_regen_loop_timer().on_insert(...)`.
pub struct EnemyRegenLoopTimerTableHandle<'ctx> {
    imp: __sdk::TableHandle<EnemyRegenLoopTimer>,
    ctx: std::marker::PhantomData<&'ctx super::RemoteTables>,
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the table `enemy_regen_loop_timer`.
///
/// Implemented for [`super::RemoteTables`].
pub trait EnemyRegenLoopTimerTableAccess {
    #[allow(non_snake_case)]
    /// Obtain a [`EnemyRegenLoopTimerTableHandle`], which mediates access to the table `enemy_regen_loop_timer`.
    fn enemy_regen_loop_timer(&self) -> EnemyRegenLoopTimerTableHandle<'_>;
}

impl EnemyRegenLoopTimerTableAccess for super::RemoteTables {
    fn enemy_regen_loop_timer(&self) -> EnemyRegenLoopTimerTableHandle<'_> {
        EnemyRegenLoopTimerTableHandle {
            imp: self
                .imp
                .get_table::<EnemyRegenLoopTimer>("enemy_regen_loop_timer"),
            ctx: std::marker::PhantomData,
        }
    }
}

pub struct EnemyRegenLoopTimerInsertCallbackId(__sdk::CallbackId);
pub struct EnemyRegenLoopTimerDeleteCallbackId(__sdk::CallbackId);

impl<'ctx> __sdk::Table for EnemyRegenLoopTimerTableHandle<'ctx> {
    type Row = EnemyRegenLoopTimer;
    type EventContext = super::EventContext;

    fn count(&self) -> u64 {
        self.imp.count()
    }
    fn iter(&self) -> impl Iterator<Item = EnemyRegenLoopTimer> + '_ {
        self.imp.iter()
    }

    type InsertCallbackId = EnemyRegenLoopTimerInsertCallbackId;

    fn on_insert(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row) + Send + 'static,
    ) -> EnemyRegenLoopTimerInsertCallbackId {
        EnemyRegenLoopTimerInsertCallbackId(self.imp.on_insert(Box::new(callback)))
    }

    fn remove_on_insert(&self, callback: EnemyRegenLoopTimerInsertCallbackId) {
        self.imp.remove_on_insert(callback.0)
    }

    type DeleteCallbackId = EnemyRegenLoopTimerDeleteCallbackId;

    fn on_delete(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row) + Send + 'static,
    ) -> EnemyRegenLoopTimerDeleteCallbackId {
        EnemyRegenLoopTimerDeleteCallbackId(self.imp.on_delete(Box::new(callback)))
    }

    fn remove_on_delete(&self, callback: EnemyRegenLoopTimerDeleteCallbackId) {
        self.imp.remove_on_delete(callback.0)
    }
}

#[doc(hidden)]
pub(super) fn register_table(client_cache: &mut __sdk::ClientCache<super::RemoteModule>) {
    let _table = client_cache.get_or_make_table::<EnemyRegenLoopTimer>("enemy_regen_loop_timer");
    _table.add_unique_constraint::<u64>("scheduled_id", |row| &row.scheduled_id);
}
pub struct EnemyRegenLoopTimerUpdateCallbackId(__sdk::CallbackId);

impl<'ctx> __sdk::TableWithPrimaryKey for EnemyRegenLoopTimerTableHandle<'ctx> {
    type UpdateCallbackId = EnemyRegenLoopTimerUpdateCallbackId;

    fn on_update(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row, &Self::Row) + Send + 'static,
    ) -> EnemyRegenLoopTimerUpdateCallbackId {
        EnemyRegenLoopTimerUpdateCallbackId(self.imp.on_update(Box::new(callback)))
    }

    fn remove_on_update(&self, callback: EnemyRegenLoopTimerUpdateCallbackId) {
        self.imp.remove_on_update(callback.0)
    }
}

#[doc(hidden)]
pub(super) fn parse_table_update(
    raw_updates: __ws::TableUpdate<__ws::BsatnFormat>,
) -> __sdk::Result<__sdk::TableUpdate<EnemyRegenLoopTimer>> {
    __sdk::TableUpdate::parse_table_update(raw_updates).map_err(|e| {
        __sdk::InternalError::failed_parse("TableUpdate<EnemyRegenLoopTimer>", "TableUpdate")
            .with_cause(e)
            .into()
    })
}

/// Access to the `scheduled_id` unique index on the table `enemy_regen_loop_timer`,
/// which allows point queries on the field of the same name
/// via the [`EnemyRegenLoopTimerScheduledIdUnique::find`] method.
///
/// Users are encouraged not to explicitly reference this type,
/// but to directly chain method calls,
/// like `ctx.db.enemy_regen_loop_timer().scheduled_id().find(...)`.
pub struct EnemyRegenLoopTimerScheduledIdUnique<'ctx> {
    imp: __sdk::UniqueConstraintHandle<EnemyRegenLoopTimer, u64>,
    phantom: std::marker::PhantomData<&'ctx super::RemoteTables>,
}

impl<'ctx> EnemyRegenLoopTimerTableHandle<'ctx> {
    /// Get a handle on the `scheduled_id` unique index on the table `enemy_regen_loop_timer`.
    pub fn scheduled_id(&self) -> EnemyRegenLoopTimerScheduledIdUnique<'ctx> {
        EnemyRegenLoopTimerScheduledIdUnique {
            imp: self.imp.get_unique_constraint::<u64>("scheduled_id"),
            phantom: std::marker::PhantomData,
        }
    }
}

impl<'ctx> EnemyRegenLoopTimerScheduledIdUnique<'ctx> {
    /// Find the subscribed row whose `scheduled_id` column value is equal to `col_val`,
    /// if such a row is present in the client cache.
    pub fn find(&self, col_val: &u64) -> Option<EnemyRegenLoopTimer> {
        self.imp.find(col_val)
    }
}
