// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

use super::building_despawn_timer_type::BuildingDespawnTimer;

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct BuildingDespawnArgs {
    pub timer: BuildingDespawnTimer,
}

impl From<BuildingDespawnArgs> for super::Reducer {
    fn from(args: BuildingDespawnArgs) -> Self {
        Self::BuildingDespawn { timer: args.timer }
    }
}

impl __sdk::InModule for BuildingDespawnArgs {
    type Module = super::RemoteModule;
}

pub struct BuildingDespawnCallbackId(__sdk::CallbackId);

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `building_despawn`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait building_despawn {
    /// Request that the remote module invoke the reducer `building_despawn` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed by listening for [`Self::on_building_despawn`] callbacks.
    fn building_despawn(&self, timer: BuildingDespawnTimer) -> __sdk::Result<()>;
    /// Register a callback to run whenever we are notified of an invocation of the reducer `building_despawn`.
    ///
    /// Callbacks should inspect the [`__sdk::ReducerEvent`] contained in the [`super::ReducerEventContext`]
    /// to determine the reducer's status.
    ///
    /// The returned [`BuildingDespawnCallbackId`] can be passed to [`Self::remove_on_building_despawn`]
    /// to cancel the callback.
    fn on_building_despawn(
        &self,
        callback: impl FnMut(&super::ReducerEventContext, &BuildingDespawnTimer) + Send + 'static,
    ) -> BuildingDespawnCallbackId;
    /// Cancel a callback previously registered by [`Self::on_building_despawn`],
    /// causing it not to run in the future.
    fn remove_on_building_despawn(&self, callback: BuildingDespawnCallbackId);
}

impl building_despawn for super::RemoteReducers {
    fn building_despawn(&self, timer: BuildingDespawnTimer) -> __sdk::Result<()> {
        self.imp
            .call_reducer("building_despawn", BuildingDespawnArgs { timer })
    }
    fn on_building_despawn(
        &self,
        mut callback: impl FnMut(&super::ReducerEventContext, &BuildingDespawnTimer) + Send + 'static,
    ) -> BuildingDespawnCallbackId {
        BuildingDespawnCallbackId(self.imp.on_reducer(
            "building_despawn",
            Box::new(move |ctx: &super::ReducerEventContext| {
                #[allow(irrefutable_let_patterns)]
                let super::ReducerEventContext {
                    event:
                        __sdk::ReducerEvent {
                            reducer: super::Reducer::BuildingDespawn { timer },
                            ..
                        },
                    ..
                } = ctx
                else {
                    unreachable!()
                };
                callback(ctx, timer)
            }),
        ))
    }
    fn remove_on_building_despawn(&self, callback: BuildingDespawnCallbackId) {
        self.imp.remove_on_reducer("building_despawn", callback.0)
    }
}

#[allow(non_camel_case_types)]
#[doc(hidden)]
/// Extension trait for setting the call-flags for the reducer `building_despawn`.
///
/// Implemented for [`super::SetReducerFlags`].
///
/// This type is currently unstable and may be removed without a major version bump.
pub trait set_flags_for_building_despawn {
    /// Set the call-reducer flags for the reducer `building_despawn` to `flags`.
    ///
    /// This type is currently unstable and may be removed without a major version bump.
    fn building_despawn(&self, flags: __ws::CallReducerFlags);
}

impl set_flags_for_building_despawn for super::SetReducerFlags {
    fn building_despawn(&self, flags: __ws::CallReducerFlags) {
        self.imp.set_call_reducer_flags("building_despawn", flags);
    }
}
