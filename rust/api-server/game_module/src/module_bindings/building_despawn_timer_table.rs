// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use super::building_despawn_timer_type::BuildingDespawnTimer;
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

/// Table handle for the table `building_despawn_timer`.
///
/// Obtain a handle from the [`BuildingDespawnTimerTableAccess::building_despawn_timer`] method on [`super::RemoteTables`],
/// like `ctx.db.building_despawn_timer()`.
///
/// Users are encouraged not to explicitly reference this type,
/// but to directly chain method calls,
/// like `ctx.db.building_despawn_timer().on_insert(...)`.
pub struct BuildingDespawnTimerTableHandle<'ctx> {
    imp: __sdk::TableHandle<BuildingDespawnTimer>,
    ctx: std::marker::PhantomData<&'ctx super::RemoteTables>,
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the table `building_despawn_timer`.
///
/// Implemented for [`super::RemoteTables`].
pub trait BuildingDespawnTimerTableAccess {
    #[allow(non_snake_case)]
    /// Obtain a [`BuildingDespawnTimerTableHandle`], which mediates access to the table `building_despawn_timer`.
    fn building_despawn_timer(&self) -> BuildingDespawnTimerTableHandle<'_>;
}

impl BuildingDespawnTimerTableAccess for super::RemoteTables {
    fn building_despawn_timer(&self) -> BuildingDespawnTimerTableHandle<'_> {
        BuildingDespawnTimerTableHandle {
            imp: self
                .imp
                .get_table::<BuildingDespawnTimer>("building_despawn_timer"),
            ctx: std::marker::PhantomData,
        }
    }
}

pub struct BuildingDespawnTimerInsertCallbackId(__sdk::CallbackId);
pub struct BuildingDespawnTimerDeleteCallbackId(__sdk::CallbackId);

impl<'ctx> __sdk::Table for BuildingDespawnTimerTableHandle<'ctx> {
    type Row = BuildingDespawnTimer;
    type EventContext = super::EventContext;

    fn count(&self) -> u64 {
        self.imp.count()
    }
    fn iter(&self) -> impl Iterator<Item = BuildingDespawnTimer> + '_ {
        self.imp.iter()
    }

    type InsertCallbackId = BuildingDespawnTimerInsertCallbackId;

    fn on_insert(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row) + Send + 'static,
    ) -> BuildingDespawnTimerInsertCallbackId {
        BuildingDespawnTimerInsertCallbackId(self.imp.on_insert(Box::new(callback)))
    }

    fn remove_on_insert(&self, callback: BuildingDespawnTimerInsertCallbackId) {
        self.imp.remove_on_insert(callback.0)
    }

    type DeleteCallbackId = BuildingDespawnTimerDeleteCallbackId;

    fn on_delete(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row) + Send + 'static,
    ) -> BuildingDespawnTimerDeleteCallbackId {
        BuildingDespawnTimerDeleteCallbackId(self.imp.on_delete(Box::new(callback)))
    }

    fn remove_on_delete(&self, callback: BuildingDespawnTimerDeleteCallbackId) {
        self.imp.remove_on_delete(callback.0)
    }
}

#[doc(hidden)]
pub(super) fn register_table(client_cache: &mut __sdk::ClientCache<super::RemoteModule>) {
    let _table = client_cache.get_or_make_table::<BuildingDespawnTimer>("building_despawn_timer");
    _table.add_unique_constraint::<u64>("scheduled_id", |row| &row.scheduled_id);
}
pub struct BuildingDespawnTimerUpdateCallbackId(__sdk::CallbackId);

impl<'ctx> __sdk::TableWithPrimaryKey for BuildingDespawnTimerTableHandle<'ctx> {
    type UpdateCallbackId = BuildingDespawnTimerUpdateCallbackId;

    fn on_update(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row, &Self::Row) + Send + 'static,
    ) -> BuildingDespawnTimerUpdateCallbackId {
        BuildingDespawnTimerUpdateCallbackId(self.imp.on_update(Box::new(callback)))
    }

    fn remove_on_update(&self, callback: BuildingDespawnTimerUpdateCallbackId) {
        self.imp.remove_on_update(callback.0)
    }
}

#[doc(hidden)]
pub(super) fn parse_table_update(
    raw_updates: __ws::TableUpdate<__ws::BsatnFormat>,
) -> __sdk::Result<__sdk::TableUpdate<BuildingDespawnTimer>> {
    __sdk::TableUpdate::parse_table_update(raw_updates).map_err(|e| {
        __sdk::InternalError::failed_parse("TableUpdate<BuildingDespawnTimer>", "TableUpdate")
            .with_cause(e)
            .into()
    })
}

/// Access to the `scheduled_id` unique index on the table `building_despawn_timer`,
/// which allows point queries on the field of the same name
/// via the [`BuildingDespawnTimerScheduledIdUnique::find`] method.
///
/// Users are encouraged not to explicitly reference this type,
/// but to directly chain method calls,
/// like `ctx.db.building_despawn_timer().scheduled_id().find(...)`.
pub struct BuildingDespawnTimerScheduledIdUnique<'ctx> {
    imp: __sdk::UniqueConstraintHandle<BuildingDespawnTimer, u64>,
    phantom: std::marker::PhantomData<&'ctx super::RemoteTables>,
}

impl<'ctx> BuildingDespawnTimerTableHandle<'ctx> {
    /// Get a handle on the `scheduled_id` unique index on the table `building_despawn_timer`.
    pub fn scheduled_id(&self) -> BuildingDespawnTimerScheduledIdUnique<'ctx> {
        BuildingDespawnTimerScheduledIdUnique {
            imp: self.imp.get_unique_constraint::<u64>("scheduled_id"),
            phantom: std::marker::PhantomData,
        }
    }
}

impl<'ctx> BuildingDespawnTimerScheduledIdUnique<'ctx> {
    /// Find the subscribed row whose `scheduled_id` column value is equal to `col_val`,
    /// if such a row is present in the client cache.
    pub fn find(&self, col_val: &u64) -> Option<BuildingDespawnTimer> {
        self.imp.find(col_val)
    }
}
