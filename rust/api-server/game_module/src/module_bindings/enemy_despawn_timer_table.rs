// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use super::enemy_despawn_timer_type::EnemyDespawnTimer;
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

/// Table handle for the table `enemy_despawn_timer`.
///
/// Obtain a handle from the [`EnemyDespawnTimerTableAccess::enemy_despawn_timer`] method on [`super::RemoteTables`],
/// like `ctx.db.enemy_despawn_timer()`.
///
/// Users are encouraged not to explicitly reference this type,
/// but to directly chain method calls,
/// like `ctx.db.enemy_despawn_timer().on_insert(...)`.
pub struct EnemyDespawnTimerTableHandle<'ctx> {
    imp: __sdk::TableHandle<EnemyDespawnTimer>,
    ctx: std::marker::PhantomData<&'ctx super::RemoteTables>,
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the table `enemy_despawn_timer`.
///
/// Implemented for [`super::RemoteTables`].
pub trait EnemyDespawnTimerTableAccess {
    #[allow(non_snake_case)]
    /// Obtain a [`EnemyDespawnTimerTableHandle`], which mediates access to the table `enemy_despawn_timer`.
    fn enemy_despawn_timer(&self) -> EnemyDespawnTimerTableHandle<'_>;
}

impl EnemyDespawnTimerTableAccess for super::RemoteTables {
    fn enemy_despawn_timer(&self) -> EnemyDespawnTimerTableHandle<'_> {
        EnemyDespawnTimerTableHandle {
            imp: self
                .imp
                .get_table::<EnemyDespawnTimer>("enemy_despawn_timer"),
            ctx: std::marker::PhantomData,
        }
    }
}

pub struct EnemyDespawnTimerInsertCallbackId(__sdk::CallbackId);
pub struct EnemyDespawnTimerDeleteCallbackId(__sdk::CallbackId);

impl<'ctx> __sdk::Table for EnemyDespawnTimerTableHandle<'ctx> {
    type Row = EnemyDespawnTimer;
    type EventContext = super::EventContext;

    fn count(&self) -> u64 {
        self.imp.count()
    }
    fn iter(&self) -> impl Iterator<Item = EnemyDespawnTimer> + '_ {
        self.imp.iter()
    }

    type InsertCallbackId = EnemyDespawnTimerInsertCallbackId;

    fn on_insert(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row) + Send + 'static,
    ) -> EnemyDespawnTimerInsertCallbackId {
        EnemyDespawnTimerInsertCallbackId(self.imp.on_insert(Box::new(callback)))
    }

    fn remove_on_insert(&self, callback: EnemyDespawnTimerInsertCallbackId) {
        self.imp.remove_on_insert(callback.0)
    }

    type DeleteCallbackId = EnemyDespawnTimerDeleteCallbackId;

    fn on_delete(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row) + Send + 'static,
    ) -> EnemyDespawnTimerDeleteCallbackId {
        EnemyDespawnTimerDeleteCallbackId(self.imp.on_delete(Box::new(callback)))
    }

    fn remove_on_delete(&self, callback: EnemyDespawnTimerDeleteCallbackId) {
        self.imp.remove_on_delete(callback.0)
    }
}

#[doc(hidden)]
pub(super) fn register_table(client_cache: &mut __sdk::ClientCache<super::RemoteModule>) {
    let _table = client_cache.get_or_make_table::<EnemyDespawnTimer>("enemy_despawn_timer");
    _table.add_unique_constraint::<u64>("scheduled_id", |row| &row.scheduled_id);
}
pub struct EnemyDespawnTimerUpdateCallbackId(__sdk::CallbackId);

impl<'ctx> __sdk::TableWithPrimaryKey for EnemyDespawnTimerTableHandle<'ctx> {
    type UpdateCallbackId = EnemyDespawnTimerUpdateCallbackId;

    fn on_update(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row, &Self::Row) + Send + 'static,
    ) -> EnemyDespawnTimerUpdateCallbackId {
        EnemyDespawnTimerUpdateCallbackId(self.imp.on_update(Box::new(callback)))
    }

    fn remove_on_update(&self, callback: EnemyDespawnTimerUpdateCallbackId) {
        self.imp.remove_on_update(callback.0)
    }
}

#[doc(hidden)]
pub(super) fn parse_table_update(
    raw_updates: __ws::TableUpdate<__ws::BsatnFormat>,
) -> __sdk::Result<__sdk::TableUpdate<EnemyDespawnTimer>> {
    __sdk::TableUpdate::parse_table_update(raw_updates).map_err(|e| {
        __sdk::InternalError::failed_parse("TableUpdate<EnemyDespawnTimer>", "TableUpdate")
            .with_cause(e)
            .into()
    })
}

/// Access to the `scheduled_id` unique index on the table `enemy_despawn_timer`,
/// which allows point queries on the field of the same name
/// via the [`EnemyDespawnTimerScheduledIdUnique::find`] method.
///
/// Users are encouraged not to explicitly reference this type,
/// but to directly chain method calls,
/// like `ctx.db.enemy_despawn_timer().scheduled_id().find(...)`.
pub struct EnemyDespawnTimerScheduledIdUnique<'ctx> {
    imp: __sdk::UniqueConstraintHandle<EnemyDespawnTimer, u64>,
    phantom: std::marker::PhantomData<&'ctx super::RemoteTables>,
}

impl<'ctx> EnemyDespawnTimerTableHandle<'ctx> {
    /// Get a handle on the `scheduled_id` unique index on the table `enemy_despawn_timer`.
    pub fn scheduled_id(&self) -> EnemyDespawnTimerScheduledIdUnique<'ctx> {
        EnemyDespawnTimerScheduledIdUnique {
            imp: self.imp.get_unique_constraint::<u64>("scheduled_id"),
            phantom: std::marker::PhantomData,
        }
    }
}

impl<'ctx> EnemyDespawnTimerScheduledIdUnique<'ctx> {
    /// Find the subscribed row whose `scheduled_id` column value is equal to `col_val`,
    /// if such a row is present in the client cache.
    pub fn find(&self, col_val: &u64) -> Option<EnemyDespawnTimer> {
        self.imp.find(col_val)
    }
}

#[allow(non_camel_case_types)]
/// Extension trait for query builder access to the table `EnemyDespawnTimer`.
///
/// Implemented for [`__sdk::QueryTableAccessor`].
pub trait enemy_despawn_timerQueryTableAccess {
    #[allow(non_snake_case)]
    /// Get a query builder for the table `EnemyDespawnTimer`.
    fn enemy_despawn_timer(&self) -> __sdk::__query_builder::Table<EnemyDespawnTimer>;
}

impl enemy_despawn_timerQueryTableAccess for __sdk::QueryTableAccessor {
    fn enemy_despawn_timer(&self) -> __sdk::__query_builder::Table<EnemyDespawnTimer> {
        __sdk::__query_builder::Table::new("enemy_despawn_timer")
    }
}
