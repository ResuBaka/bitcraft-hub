// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

use super::enemy_despawn_timer_type::EnemyDespawnTimer;

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct EnemyDespawnArgs {
    pub timer: EnemyDespawnTimer,
}

impl From<EnemyDespawnArgs> for super::Reducer {
    fn from(args: EnemyDespawnArgs) -> Self {
        Self::EnemyDespawn { timer: args.timer }
    }
}

impl __sdk::InModule for EnemyDespawnArgs {
    type Module = super::RemoteModule;
}

pub struct EnemyDespawnCallbackId(__sdk::CallbackId);

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `enemy_despawn`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait enemy_despawn {
    /// Request that the remote module invoke the reducer `enemy_despawn` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed by listening for [`Self::on_enemy_despawn`] callbacks.
    fn enemy_despawn(&self, timer: EnemyDespawnTimer) -> __sdk::Result<()>;
    /// Register a callback to run whenever we are notified of an invocation of the reducer `enemy_despawn`.
    ///
    /// Callbacks should inspect the [`__sdk::ReducerEvent`] contained in the [`super::ReducerEventContext`]
    /// to determine the reducer's status.
    ///
    /// The returned [`EnemyDespawnCallbackId`] can be passed to [`Self::remove_on_enemy_despawn`]
    /// to cancel the callback.
    fn on_enemy_despawn(
        &self,
        callback: impl FnMut(&super::ReducerEventContext, &EnemyDespawnTimer) + Send + 'static,
    ) -> EnemyDespawnCallbackId;
    /// Cancel a callback previously registered by [`Self::on_enemy_despawn`],
    /// causing it not to run in the future.
    fn remove_on_enemy_despawn(&self, callback: EnemyDespawnCallbackId);
}

impl enemy_despawn for super::RemoteReducers {
    fn enemy_despawn(&self, timer: EnemyDespawnTimer) -> __sdk::Result<()> {
        self.imp
            .call_reducer("enemy_despawn", EnemyDespawnArgs { timer })
    }
    fn on_enemy_despawn(
        &self,
        mut callback: impl FnMut(&super::ReducerEventContext, &EnemyDespawnTimer) + Send + 'static,
    ) -> EnemyDespawnCallbackId {
        EnemyDespawnCallbackId(self.imp.on_reducer(
            "enemy_despawn",
            Box::new(move |ctx: &super::ReducerEventContext| {
                #[allow(irrefutable_let_patterns)]
                let super::ReducerEventContext {
                    event:
                        __sdk::ReducerEvent {
                            reducer: super::Reducer::EnemyDespawn { timer },
                            ..
                        },
                    ..
                } = ctx
                else {
                    unreachable!()
                };
                callback(ctx, timer)
            }),
        ))
    }
    fn remove_on_enemy_despawn(&self, callback: EnemyDespawnCallbackId) {
        self.imp.remove_on_reducer("enemy_despawn", callback.0)
    }
}

#[allow(non_camel_case_types)]
#[doc(hidden)]
/// Extension trait for setting the call-flags for the reducer `enemy_despawn`.
///
/// Implemented for [`super::SetReducerFlags`].
///
/// This type is currently unstable and may be removed without a major version bump.
pub trait set_flags_for_enemy_despawn {
    /// Set the call-reducer flags for the reducer `enemy_despawn` to `flags`.
    ///
    /// This type is currently unstable and may be removed without a major version bump.
    fn enemy_despawn(&self, flags: __ws::CallReducerFlags);
}

impl set_flags_for_enemy_despawn for super::SetReducerFlags {
    fn enemy_despawn(&self, flags: __ws::CallReducerFlags) {
        self.imp.set_call_reducer_flags("enemy_despawn", flags);
    }
}
