// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

// This was generated using spacetimedb cli version 1.2.0 (commit fb41e50eb73573b70eea532aeb6158eaac06fae0).

#![allow(unused, clippy::all)]
use super::loot_chest_spawn_timer_type::LootChestSpawnTimer;
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

/// Table handle for the table `loot_chest_spawn_timer`.
///
/// Obtain a handle from the [`LootChestSpawnTimerTableAccess::loot_chest_spawn_timer`] method on [`super::RemoteTables`],
/// like `ctx.db.loot_chest_spawn_timer()`.
///
/// Users are encouraged not to explicitly reference this type,
/// but to directly chain method calls,
/// like `ctx.db.loot_chest_spawn_timer().on_insert(...)`.
pub struct LootChestSpawnTimerTableHandle<'ctx> {
    imp: __sdk::TableHandle<LootChestSpawnTimer>,
    ctx: std::marker::PhantomData<&'ctx super::RemoteTables>,
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the table `loot_chest_spawn_timer`.
///
/// Implemented for [`super::RemoteTables`].
pub trait LootChestSpawnTimerTableAccess {
    #[allow(non_snake_case)]
    /// Obtain a [`LootChestSpawnTimerTableHandle`], which mediates access to the table `loot_chest_spawn_timer`.
    fn loot_chest_spawn_timer(&self) -> LootChestSpawnTimerTableHandle<'_>;
}

impl LootChestSpawnTimerTableAccess for super::RemoteTables {
    fn loot_chest_spawn_timer(&self) -> LootChestSpawnTimerTableHandle<'_> {
        LootChestSpawnTimerTableHandle {
            imp: self
                .imp
                .get_table::<LootChestSpawnTimer>("loot_chest_spawn_timer"),
            ctx: std::marker::PhantomData,
        }
    }
}

pub struct LootChestSpawnTimerInsertCallbackId(__sdk::CallbackId);
pub struct LootChestSpawnTimerDeleteCallbackId(__sdk::CallbackId);

impl<'ctx> __sdk::Table for LootChestSpawnTimerTableHandle<'ctx> {
    type Row = LootChestSpawnTimer;
    type EventContext = super::EventContext;

    fn count(&self) -> u64 {
        self.imp.count()
    }
    fn iter(&self) -> impl Iterator<Item = LootChestSpawnTimer> + '_ {
        self.imp.iter()
    }

    type InsertCallbackId = LootChestSpawnTimerInsertCallbackId;

    fn on_insert(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row) + Send + 'static,
    ) -> LootChestSpawnTimerInsertCallbackId {
        LootChestSpawnTimerInsertCallbackId(self.imp.on_insert(Box::new(callback)))
    }

    fn remove_on_insert(&self, callback: LootChestSpawnTimerInsertCallbackId) {
        self.imp.remove_on_insert(callback.0)
    }

    type DeleteCallbackId = LootChestSpawnTimerDeleteCallbackId;

    fn on_delete(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row) + Send + 'static,
    ) -> LootChestSpawnTimerDeleteCallbackId {
        LootChestSpawnTimerDeleteCallbackId(self.imp.on_delete(Box::new(callback)))
    }

    fn remove_on_delete(&self, callback: LootChestSpawnTimerDeleteCallbackId) {
        self.imp.remove_on_delete(callback.0)
    }
}

#[doc(hidden)]
pub(super) fn register_table(client_cache: &mut __sdk::ClientCache<super::RemoteModule>) {
    let _table = client_cache.get_or_make_table::<LootChestSpawnTimer>("loot_chest_spawn_timer");
    _table.add_unique_constraint::<u64>("scheduled_id", |row| &row.scheduled_id);
}
pub struct LootChestSpawnTimerUpdateCallbackId(__sdk::CallbackId);

impl<'ctx> __sdk::TableWithPrimaryKey for LootChestSpawnTimerTableHandle<'ctx> {
    type UpdateCallbackId = LootChestSpawnTimerUpdateCallbackId;

    fn on_update(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row, &Self::Row) + Send + 'static,
    ) -> LootChestSpawnTimerUpdateCallbackId {
        LootChestSpawnTimerUpdateCallbackId(self.imp.on_update(Box::new(callback)))
    }

    fn remove_on_update(&self, callback: LootChestSpawnTimerUpdateCallbackId) {
        self.imp.remove_on_update(callback.0)
    }
}

#[doc(hidden)]
pub(super) fn parse_table_update(
    raw_updates: __ws::TableUpdate<__ws::BsatnFormat>,
) -> __sdk::Result<__sdk::TableUpdate<LootChestSpawnTimer>> {
    __sdk::TableUpdate::parse_table_update(raw_updates).map_err(|e| {
        __sdk::InternalError::failed_parse("TableUpdate<LootChestSpawnTimer>", "TableUpdate")
            .with_cause(e)
            .into()
    })
}

/// Access to the `scheduled_id` unique index on the table `loot_chest_spawn_timer`,
/// which allows point queries on the field of the same name
/// via the [`LootChestSpawnTimerScheduledIdUnique::find`] method.
///
/// Users are encouraged not to explicitly reference this type,
/// but to directly chain method calls,
/// like `ctx.db.loot_chest_spawn_timer().scheduled_id().find(...)`.
pub struct LootChestSpawnTimerScheduledIdUnique<'ctx> {
    imp: __sdk::UniqueConstraintHandle<LootChestSpawnTimer, u64>,
    phantom: std::marker::PhantomData<&'ctx super::RemoteTables>,
}

impl<'ctx> LootChestSpawnTimerTableHandle<'ctx> {
    /// Get a handle on the `scheduled_id` unique index on the table `loot_chest_spawn_timer`.
    pub fn scheduled_id(&self) -> LootChestSpawnTimerScheduledIdUnique<'ctx> {
        LootChestSpawnTimerScheduledIdUnique {
            imp: self.imp.get_unique_constraint::<u64>("scheduled_id"),
            phantom: std::marker::PhantomData,
        }
    }
}

impl<'ctx> LootChestSpawnTimerScheduledIdUnique<'ctx> {
    /// Find the subscribed row whose `scheduled_id` column value is equal to `col_val`,
    /// if such a row is present in the client cache.
    pub fn find(&self, col_val: &u64) -> Option<LootChestSpawnTimer> {
        self.imp.find(col_val)
    }
}
