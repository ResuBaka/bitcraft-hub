// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use super::hide_deployable_timer_type::HideDeployableTimer;
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

/// Table handle for the table `hide_deployable_timer`.
///
/// Obtain a handle from the [`HideDeployableTimerTableAccess::hide_deployable_timer`] method on [`super::RemoteTables`],
/// like `ctx.db.hide_deployable_timer()`.
///
/// Users are encouraged not to explicitly reference this type,
/// but to directly chain method calls,
/// like `ctx.db.hide_deployable_timer().on_insert(...)`.
pub struct HideDeployableTimerTableHandle<'ctx> {
    imp: __sdk::TableHandle<HideDeployableTimer>,
    ctx: std::marker::PhantomData<&'ctx super::RemoteTables>,
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the table `hide_deployable_timer`.
///
/// Implemented for [`super::RemoteTables`].
pub trait HideDeployableTimerTableAccess {
    #[allow(non_snake_case)]
    /// Obtain a [`HideDeployableTimerTableHandle`], which mediates access to the table `hide_deployable_timer`.
    fn hide_deployable_timer(&self) -> HideDeployableTimerTableHandle<'_>;
}

impl HideDeployableTimerTableAccess for super::RemoteTables {
    fn hide_deployable_timer(&self) -> HideDeployableTimerTableHandle<'_> {
        HideDeployableTimerTableHandle {
            imp: self
                .imp
                .get_table::<HideDeployableTimer>("hide_deployable_timer"),
            ctx: std::marker::PhantomData,
        }
    }
}

pub struct HideDeployableTimerInsertCallbackId(__sdk::CallbackId);
pub struct HideDeployableTimerDeleteCallbackId(__sdk::CallbackId);

impl<'ctx> __sdk::Table for HideDeployableTimerTableHandle<'ctx> {
    type Row = HideDeployableTimer;
    type EventContext = super::EventContext;

    fn count(&self) -> u64 {
        self.imp.count()
    }
    fn iter(&self) -> impl Iterator<Item = HideDeployableTimer> + '_ {
        self.imp.iter()
    }

    type InsertCallbackId = HideDeployableTimerInsertCallbackId;

    fn on_insert(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row) + Send + 'static,
    ) -> HideDeployableTimerInsertCallbackId {
        HideDeployableTimerInsertCallbackId(self.imp.on_insert(Box::new(callback)))
    }

    fn remove_on_insert(&self, callback: HideDeployableTimerInsertCallbackId) {
        self.imp.remove_on_insert(callback.0)
    }

    type DeleteCallbackId = HideDeployableTimerDeleteCallbackId;

    fn on_delete(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row) + Send + 'static,
    ) -> HideDeployableTimerDeleteCallbackId {
        HideDeployableTimerDeleteCallbackId(self.imp.on_delete(Box::new(callback)))
    }

    fn remove_on_delete(&self, callback: HideDeployableTimerDeleteCallbackId) {
        self.imp.remove_on_delete(callback.0)
    }
}

#[doc(hidden)]
pub(super) fn register_table(client_cache: &mut __sdk::ClientCache<super::RemoteModule>) {
    let _table = client_cache.get_or_make_table::<HideDeployableTimer>("hide_deployable_timer");
    _table.add_unique_constraint::<u64>("scheduled_id", |row| &row.scheduled_id);
    _table.add_unique_constraint::<u64>("entity_id", |row| &row.entity_id);
}
pub struct HideDeployableTimerUpdateCallbackId(__sdk::CallbackId);

impl<'ctx> __sdk::TableWithPrimaryKey for HideDeployableTimerTableHandle<'ctx> {
    type UpdateCallbackId = HideDeployableTimerUpdateCallbackId;

    fn on_update(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row, &Self::Row) + Send + 'static,
    ) -> HideDeployableTimerUpdateCallbackId {
        HideDeployableTimerUpdateCallbackId(self.imp.on_update(Box::new(callback)))
    }

    fn remove_on_update(&self, callback: HideDeployableTimerUpdateCallbackId) {
        self.imp.remove_on_update(callback.0)
    }
}

#[doc(hidden)]
pub(super) fn parse_table_update(
    raw_updates: __ws::TableUpdate<__ws::BsatnFormat>,
) -> __sdk::Result<__sdk::TableUpdate<HideDeployableTimer>> {
    __sdk::TableUpdate::parse_table_update(raw_updates).map_err(|e| {
        __sdk::InternalError::failed_parse("TableUpdate<HideDeployableTimer>", "TableUpdate")
            .with_cause(e)
            .into()
    })
}

/// Access to the `scheduled_id` unique index on the table `hide_deployable_timer`,
/// which allows point queries on the field of the same name
/// via the [`HideDeployableTimerScheduledIdUnique::find`] method.
///
/// Users are encouraged not to explicitly reference this type,
/// but to directly chain method calls,
/// like `ctx.db.hide_deployable_timer().scheduled_id().find(...)`.
pub struct HideDeployableTimerScheduledIdUnique<'ctx> {
    imp: __sdk::UniqueConstraintHandle<HideDeployableTimer, u64>,
    phantom: std::marker::PhantomData<&'ctx super::RemoteTables>,
}

impl<'ctx> HideDeployableTimerTableHandle<'ctx> {
    /// Get a handle on the `scheduled_id` unique index on the table `hide_deployable_timer`.
    pub fn scheduled_id(&self) -> HideDeployableTimerScheduledIdUnique<'ctx> {
        HideDeployableTimerScheduledIdUnique {
            imp: self.imp.get_unique_constraint::<u64>("scheduled_id"),
            phantom: std::marker::PhantomData,
        }
    }
}

impl<'ctx> HideDeployableTimerScheduledIdUnique<'ctx> {
    /// Find the subscribed row whose `scheduled_id` column value is equal to `col_val`,
    /// if such a row is present in the client cache.
    pub fn find(&self, col_val: &u64) -> Option<HideDeployableTimer> {
        self.imp.find(col_val)
    }
}

/// Access to the `entity_id` unique index on the table `hide_deployable_timer`,
/// which allows point queries on the field of the same name
/// via the [`HideDeployableTimerEntityIdUnique::find`] method.
///
/// Users are encouraged not to explicitly reference this type,
/// but to directly chain method calls,
/// like `ctx.db.hide_deployable_timer().entity_id().find(...)`.
pub struct HideDeployableTimerEntityIdUnique<'ctx> {
    imp: __sdk::UniqueConstraintHandle<HideDeployableTimer, u64>,
    phantom: std::marker::PhantomData<&'ctx super::RemoteTables>,
}

impl<'ctx> HideDeployableTimerTableHandle<'ctx> {
    /// Get a handle on the `entity_id` unique index on the table `hide_deployable_timer`.
    pub fn entity_id(&self) -> HideDeployableTimerEntityIdUnique<'ctx> {
        HideDeployableTimerEntityIdUnique {
            imp: self.imp.get_unique_constraint::<u64>("entity_id"),
            phantom: std::marker::PhantomData,
        }
    }
}

impl<'ctx> HideDeployableTimerEntityIdUnique<'ctx> {
    /// Find the subscribed row whose `entity_id` column value is equal to `col_val`,
    /// if such a row is present in the client cache.
    pub fn find(&self, col_val: &u64) -> Option<HideDeployableTimer> {
        self.imp.find(col_val)
    }
}

#[allow(non_camel_case_types)]
/// Extension trait for query builder access to the table `HideDeployableTimer`.
///
/// Implemented for [`__sdk::QueryTableAccessor`].
pub trait hide_deployable_timerQueryTableAccess {
    #[allow(non_snake_case)]
    /// Get a query builder for the table `HideDeployableTimer`.
    fn hide_deployable_timer(&self) -> __sdk::__query_builder::Table<HideDeployableTimer>;
}

impl hide_deployable_timerQueryTableAccess for __sdk::QueryTableAccessor {
    fn hide_deployable_timer(&self) -> __sdk::__query_builder::Table<HideDeployableTimer> {
        __sdk::__query_builder::Table::new("hide_deployable_timer")
    }
}
