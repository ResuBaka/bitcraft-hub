// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

// This was generated using spacetimedb cli version 1.2.0 (commit fb41e50eb73573b70eea532aeb6158eaac06fae0).

#![allow(unused, clippy::all)]
use super::loot_chest_despawn_timer_type::LootChestDespawnTimer;
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

/// Table handle for the table `loot_chest_despawn_timer`.
///
/// Obtain a handle from the [`LootChestDespawnTimerTableAccess::loot_chest_despawn_timer`] method on [`super::RemoteTables`],
/// like `ctx.db.loot_chest_despawn_timer()`.
///
/// Users are encouraged not to explicitly reference this type,
/// but to directly chain method calls,
/// like `ctx.db.loot_chest_despawn_timer().on_insert(...)`.
pub struct LootChestDespawnTimerTableHandle<'ctx> {
    imp: __sdk::TableHandle<LootChestDespawnTimer>,
    ctx: std::marker::PhantomData<&'ctx super::RemoteTables>,
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the table `loot_chest_despawn_timer`.
///
/// Implemented for [`super::RemoteTables`].
pub trait LootChestDespawnTimerTableAccess {
    #[allow(non_snake_case)]
    /// Obtain a [`LootChestDespawnTimerTableHandle`], which mediates access to the table `loot_chest_despawn_timer`.
    fn loot_chest_despawn_timer(&self) -> LootChestDespawnTimerTableHandle<'_>;
}

impl LootChestDespawnTimerTableAccess for super::RemoteTables {
    fn loot_chest_despawn_timer(&self) -> LootChestDespawnTimerTableHandle<'_> {
        LootChestDespawnTimerTableHandle {
            imp: self
                .imp
                .get_table::<LootChestDespawnTimer>("loot_chest_despawn_timer"),
            ctx: std::marker::PhantomData,
        }
    }
}

pub struct LootChestDespawnTimerInsertCallbackId(__sdk::CallbackId);
pub struct LootChestDespawnTimerDeleteCallbackId(__sdk::CallbackId);

impl<'ctx> __sdk::Table for LootChestDespawnTimerTableHandle<'ctx> {
    type Row = LootChestDespawnTimer;
    type EventContext = super::EventContext;

    fn count(&self) -> u64 {
        self.imp.count()
    }
    fn iter(&self) -> impl Iterator<Item = LootChestDespawnTimer> + '_ {
        self.imp.iter()
    }

    type InsertCallbackId = LootChestDespawnTimerInsertCallbackId;

    fn on_insert(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row) + Send + 'static,
    ) -> LootChestDespawnTimerInsertCallbackId {
        LootChestDespawnTimerInsertCallbackId(self.imp.on_insert(Box::new(callback)))
    }

    fn remove_on_insert(&self, callback: LootChestDespawnTimerInsertCallbackId) {
        self.imp.remove_on_insert(callback.0)
    }

    type DeleteCallbackId = LootChestDespawnTimerDeleteCallbackId;

    fn on_delete(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row) + Send + 'static,
    ) -> LootChestDespawnTimerDeleteCallbackId {
        LootChestDespawnTimerDeleteCallbackId(self.imp.on_delete(Box::new(callback)))
    }

    fn remove_on_delete(&self, callback: LootChestDespawnTimerDeleteCallbackId) {
        self.imp.remove_on_delete(callback.0)
    }
}

#[doc(hidden)]
pub(super) fn register_table(client_cache: &mut __sdk::ClientCache<super::RemoteModule>) {
    let _table =
        client_cache.get_or_make_table::<LootChestDespawnTimer>("loot_chest_despawn_timer");
    _table.add_unique_constraint::<u64>("scheduled_id", |row| &row.scheduled_id);
}
pub struct LootChestDespawnTimerUpdateCallbackId(__sdk::CallbackId);

impl<'ctx> __sdk::TableWithPrimaryKey for LootChestDespawnTimerTableHandle<'ctx> {
    type UpdateCallbackId = LootChestDespawnTimerUpdateCallbackId;

    fn on_update(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row, &Self::Row) + Send + 'static,
    ) -> LootChestDespawnTimerUpdateCallbackId {
        LootChestDespawnTimerUpdateCallbackId(self.imp.on_update(Box::new(callback)))
    }

    fn remove_on_update(&self, callback: LootChestDespawnTimerUpdateCallbackId) {
        self.imp.remove_on_update(callback.0)
    }
}

#[doc(hidden)]
pub(super) fn parse_table_update(
    raw_updates: __ws::TableUpdate<__ws::BsatnFormat>,
) -> __sdk::Result<__sdk::TableUpdate<LootChestDespawnTimer>> {
    __sdk::TableUpdate::parse_table_update(raw_updates).map_err(|e| {
        __sdk::InternalError::failed_parse("TableUpdate<LootChestDespawnTimer>", "TableUpdate")
            .with_cause(e)
            .into()
    })
}

/// Access to the `scheduled_id` unique index on the table `loot_chest_despawn_timer`,
/// which allows point queries on the field of the same name
/// via the [`LootChestDespawnTimerScheduledIdUnique::find`] method.
///
/// Users are encouraged not to explicitly reference this type,
/// but to directly chain method calls,
/// like `ctx.db.loot_chest_despawn_timer().scheduled_id().find(...)`.
pub struct LootChestDespawnTimerScheduledIdUnique<'ctx> {
    imp: __sdk::UniqueConstraintHandle<LootChestDespawnTimer, u64>,
    phantom: std::marker::PhantomData<&'ctx super::RemoteTables>,
}

impl<'ctx> LootChestDespawnTimerTableHandle<'ctx> {
    /// Get a handle on the `scheduled_id` unique index on the table `loot_chest_despawn_timer`.
    pub fn scheduled_id(&self) -> LootChestDespawnTimerScheduledIdUnique<'ctx> {
        LootChestDespawnTimerScheduledIdUnique {
            imp: self.imp.get_unique_constraint::<u64>("scheduled_id"),
            phantom: std::marker::PhantomData,
        }
    }
}

impl<'ctx> LootChestDespawnTimerScheduledIdUnique<'ctx> {
    /// Find the subscribed row whose `scheduled_id` column value is equal to `col_val`,
    /// if such a row is present in the client cache.
    pub fn find(&self, col_val: &u64) -> Option<LootChestDespawnTimer> {
        self.imp.find(col_val)
    }
}
