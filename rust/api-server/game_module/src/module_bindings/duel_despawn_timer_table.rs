// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

// This was generated using spacetimedb cli version 1.2.0 (commit fb41e50eb73573b70eea532aeb6158eaac06fae0).

#![allow(unused, clippy::all)]
use super::duel_despawn_timer_type::DuelDespawnTimer;
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

/// Table handle for the table `duel_despawn_timer`.
///
/// Obtain a handle from the [`DuelDespawnTimerTableAccess::duel_despawn_timer`] method on [`super::RemoteTables`],
/// like `ctx.db.duel_despawn_timer()`.
///
/// Users are encouraged not to explicitly reference this type,
/// but to directly chain method calls,
/// like `ctx.db.duel_despawn_timer().on_insert(...)`.
pub struct DuelDespawnTimerTableHandle<'ctx> {
    imp: __sdk::TableHandle<DuelDespawnTimer>,
    ctx: std::marker::PhantomData<&'ctx super::RemoteTables>,
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the table `duel_despawn_timer`.
///
/// Implemented for [`super::RemoteTables`].
pub trait DuelDespawnTimerTableAccess {
    #[allow(non_snake_case)]
    /// Obtain a [`DuelDespawnTimerTableHandle`], which mediates access to the table `duel_despawn_timer`.
    fn duel_despawn_timer(&self) -> DuelDespawnTimerTableHandle<'_>;
}

impl DuelDespawnTimerTableAccess for super::RemoteTables {
    fn duel_despawn_timer(&self) -> DuelDespawnTimerTableHandle<'_> {
        DuelDespawnTimerTableHandle {
            imp: self.imp.get_table::<DuelDespawnTimer>("duel_despawn_timer"),
            ctx: std::marker::PhantomData,
        }
    }
}

pub struct DuelDespawnTimerInsertCallbackId(__sdk::CallbackId);
pub struct DuelDespawnTimerDeleteCallbackId(__sdk::CallbackId);

impl<'ctx> __sdk::Table for DuelDespawnTimerTableHandle<'ctx> {
    type Row = DuelDespawnTimer;
    type EventContext = super::EventContext;

    fn count(&self) -> u64 {
        self.imp.count()
    }
    fn iter(&self) -> impl Iterator<Item = DuelDespawnTimer> + '_ {
        self.imp.iter()
    }

    type InsertCallbackId = DuelDespawnTimerInsertCallbackId;

    fn on_insert(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row) + Send + 'static,
    ) -> DuelDespawnTimerInsertCallbackId {
        DuelDespawnTimerInsertCallbackId(self.imp.on_insert(Box::new(callback)))
    }

    fn remove_on_insert(&self, callback: DuelDespawnTimerInsertCallbackId) {
        self.imp.remove_on_insert(callback.0)
    }

    type DeleteCallbackId = DuelDespawnTimerDeleteCallbackId;

    fn on_delete(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row) + Send + 'static,
    ) -> DuelDespawnTimerDeleteCallbackId {
        DuelDespawnTimerDeleteCallbackId(self.imp.on_delete(Box::new(callback)))
    }

    fn remove_on_delete(&self, callback: DuelDespawnTimerDeleteCallbackId) {
        self.imp.remove_on_delete(callback.0)
    }
}

#[doc(hidden)]
pub(super) fn register_table(client_cache: &mut __sdk::ClientCache<super::RemoteModule>) {
    let _table = client_cache.get_or_make_table::<DuelDespawnTimer>("duel_despawn_timer");
    _table.add_unique_constraint::<u64>("scheduled_id", |row| &row.scheduled_id);
}
pub struct DuelDespawnTimerUpdateCallbackId(__sdk::CallbackId);

impl<'ctx> __sdk::TableWithPrimaryKey for DuelDespawnTimerTableHandle<'ctx> {
    type UpdateCallbackId = DuelDespawnTimerUpdateCallbackId;

    fn on_update(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row, &Self::Row) + Send + 'static,
    ) -> DuelDespawnTimerUpdateCallbackId {
        DuelDespawnTimerUpdateCallbackId(self.imp.on_update(Box::new(callback)))
    }

    fn remove_on_update(&self, callback: DuelDespawnTimerUpdateCallbackId) {
        self.imp.remove_on_update(callback.0)
    }
}

#[doc(hidden)]
pub(super) fn parse_table_update(
    raw_updates: __ws::TableUpdate<__ws::BsatnFormat>,
) -> __sdk::Result<__sdk::TableUpdate<DuelDespawnTimer>> {
    __sdk::TableUpdate::parse_table_update(raw_updates).map_err(|e| {
        __sdk::InternalError::failed_parse("TableUpdate<DuelDespawnTimer>", "TableUpdate")
            .with_cause(e)
            .into()
    })
}

/// Access to the `scheduled_id` unique index on the table `duel_despawn_timer`,
/// which allows point queries on the field of the same name
/// via the [`DuelDespawnTimerScheduledIdUnique::find`] method.
///
/// Users are encouraged not to explicitly reference this type,
/// but to directly chain method calls,
/// like `ctx.db.duel_despawn_timer().scheduled_id().find(...)`.
pub struct DuelDespawnTimerScheduledIdUnique<'ctx> {
    imp: __sdk::UniqueConstraintHandle<DuelDespawnTimer, u64>,
    phantom: std::marker::PhantomData<&'ctx super::RemoteTables>,
}

impl<'ctx> DuelDespawnTimerTableHandle<'ctx> {
    /// Get a handle on the `scheduled_id` unique index on the table `duel_despawn_timer`.
    pub fn scheduled_id(&self) -> DuelDespawnTimerScheduledIdUnique<'ctx> {
        DuelDespawnTimerScheduledIdUnique {
            imp: self.imp.get_unique_constraint::<u64>("scheduled_id"),
            phantom: std::marker::PhantomData,
        }
    }
}

impl<'ctx> DuelDespawnTimerScheduledIdUnique<'ctx> {
    /// Find the subscribed row whose `scheduled_id` column value is equal to `col_val`,
    /// if such a row is present in the client cache.
    pub fn find(&self, col_val: &u64) -> Option<DuelDespawnTimer> {
        self.imp.find(col_val)
    }
}
