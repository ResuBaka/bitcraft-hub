// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use super::skill_category_type::SkillCategory;
use super::skill_desc_type::SkillDesc;
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

/// Table handle for the table `skill_desc`.
///
/// Obtain a handle from the [`SkillDescTableAccess::skill_desc`] method on [`super::RemoteTables`],
/// like `ctx.db.skill_desc()`.
///
/// Users are encouraged not to explicitly reference this type,
/// but to directly chain method calls,
/// like `ctx.db.skill_desc().on_insert(...)`.
pub struct SkillDescTableHandle<'ctx> {
    imp: __sdk::TableHandle<SkillDesc>,
    ctx: std::marker::PhantomData<&'ctx super::RemoteTables>,
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the table `skill_desc`.
///
/// Implemented for [`super::RemoteTables`].
pub trait SkillDescTableAccess {
    #[allow(non_snake_case)]
    /// Obtain a [`SkillDescTableHandle`], which mediates access to the table `skill_desc`.
    fn skill_desc(&self) -> SkillDescTableHandle<'_>;
}

impl SkillDescTableAccess for super::RemoteTables {
    fn skill_desc(&self) -> SkillDescTableHandle<'_> {
        SkillDescTableHandle {
            imp: self.imp.get_table::<SkillDesc>("skill_desc"),
            ctx: std::marker::PhantomData,
        }
    }
}

pub struct SkillDescInsertCallbackId(__sdk::CallbackId);
pub struct SkillDescDeleteCallbackId(__sdk::CallbackId);

impl<'ctx> __sdk::Table for SkillDescTableHandle<'ctx> {
    type Row = SkillDesc;
    type EventContext = super::EventContext;

    fn count(&self) -> u64 {
        self.imp.count()
    }
    fn iter(&self) -> impl Iterator<Item = SkillDesc> + '_ {
        self.imp.iter()
    }

    type InsertCallbackId = SkillDescInsertCallbackId;

    fn on_insert(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row) + Send + 'static,
    ) -> SkillDescInsertCallbackId {
        SkillDescInsertCallbackId(self.imp.on_insert(Box::new(callback)))
    }

    fn remove_on_insert(&self, callback: SkillDescInsertCallbackId) {
        self.imp.remove_on_insert(callback.0)
    }

    type DeleteCallbackId = SkillDescDeleteCallbackId;

    fn on_delete(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row) + Send + 'static,
    ) -> SkillDescDeleteCallbackId {
        SkillDescDeleteCallbackId(self.imp.on_delete(Box::new(callback)))
    }

    fn remove_on_delete(&self, callback: SkillDescDeleteCallbackId) {
        self.imp.remove_on_delete(callback.0)
    }
}

#[doc(hidden)]
pub(super) fn register_table(client_cache: &mut __sdk::ClientCache<super::RemoteModule>) {
    let _table = client_cache.get_or_make_table::<SkillDesc>("skill_desc");
    _table.add_unique_constraint::<i32>("id", |row| &row.id);
    _table.add_unique_constraint::<i32>("skill_type", |row| &row.skill_type);
}
pub struct SkillDescUpdateCallbackId(__sdk::CallbackId);

impl<'ctx> __sdk::TableWithPrimaryKey for SkillDescTableHandle<'ctx> {
    type UpdateCallbackId = SkillDescUpdateCallbackId;

    fn on_update(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row, &Self::Row) + Send + 'static,
    ) -> SkillDescUpdateCallbackId {
        SkillDescUpdateCallbackId(self.imp.on_update(Box::new(callback)))
    }

    fn remove_on_update(&self, callback: SkillDescUpdateCallbackId) {
        self.imp.remove_on_update(callback.0)
    }
}

#[doc(hidden)]
pub(super) fn parse_table_update(
    raw_updates: __ws::TableUpdate<__ws::BsatnFormat>,
) -> __sdk::Result<__sdk::TableUpdate<SkillDesc>> {
    __sdk::TableUpdate::parse_table_update(raw_updates).map_err(|e| {
        __sdk::InternalError::failed_parse("TableUpdate<SkillDesc>", "TableUpdate")
            .with_cause(e)
            .into()
    })
}

/// Access to the `id` unique index on the table `skill_desc`,
/// which allows point queries on the field of the same name
/// via the [`SkillDescIdUnique::find`] method.
///
/// Users are encouraged not to explicitly reference this type,
/// but to directly chain method calls,
/// like `ctx.db.skill_desc().id().find(...)`.
pub struct SkillDescIdUnique<'ctx> {
    imp: __sdk::UniqueConstraintHandle<SkillDesc, i32>,
    phantom: std::marker::PhantomData<&'ctx super::RemoteTables>,
}

impl<'ctx> SkillDescTableHandle<'ctx> {
    /// Get a handle on the `id` unique index on the table `skill_desc`.
    pub fn id(&self) -> SkillDescIdUnique<'ctx> {
        SkillDescIdUnique {
            imp: self.imp.get_unique_constraint::<i32>("id"),
            phantom: std::marker::PhantomData,
        }
    }
}

impl<'ctx> SkillDescIdUnique<'ctx> {
    /// Find the subscribed row whose `id` column value is equal to `col_val`,
    /// if such a row is present in the client cache.
    pub fn find(&self, col_val: &i32) -> Option<SkillDesc> {
        self.imp.find(col_val)
    }
}

/// Access to the `skill_type` unique index on the table `skill_desc`,
/// which allows point queries on the field of the same name
/// via the [`SkillDescSkillTypeUnique::find`] method.
///
/// Users are encouraged not to explicitly reference this type,
/// but to directly chain method calls,
/// like `ctx.db.skill_desc().skill_type().find(...)`.
pub struct SkillDescSkillTypeUnique<'ctx> {
    imp: __sdk::UniqueConstraintHandle<SkillDesc, i32>,
    phantom: std::marker::PhantomData<&'ctx super::RemoteTables>,
}

impl<'ctx> SkillDescTableHandle<'ctx> {
    /// Get a handle on the `skill_type` unique index on the table `skill_desc`.
    pub fn skill_type(&self) -> SkillDescSkillTypeUnique<'ctx> {
        SkillDescSkillTypeUnique {
            imp: self.imp.get_unique_constraint::<i32>("skill_type"),
            phantom: std::marker::PhantomData,
        }
    }
}

impl<'ctx> SkillDescSkillTypeUnique<'ctx> {
    /// Find the subscribed row whose `skill_type` column value is equal to `col_val`,
    /// if such a row is present in the client cache.
    pub fn find(&self, col_val: &i32) -> Option<SkillDesc> {
        self.imp.find(col_val)
    }
}
